# Easy Compositor Addon
A godot addon with utilities for compositor effects creation.

### Installation
2. Download the code as a zip file (or download the latest release if there's any).
3. Copy the folder `./addons/easy-compositor` and paste it under `res://addons/` in your Godot project.
4. Go to **Project**->**Project Settings**->**Plugins** and ensure that the *Easy Compositor* plugin is enabled.